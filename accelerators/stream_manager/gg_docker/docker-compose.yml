version: '3.7'

services:
  greengrass:
    init: true
    build:
      context: .
      dockerfile: Dockerfile-greengrass
    container_name: stream-manager-greengrass
    image: x86_64/greengrass-accelerator-stream-manager:latest
    volumes:
      - ./certs:/greengrass/certs
      - ./config:/greengrass/config
      - ./deployment:/greengrass/ggc/deployment
      - ./log:/greengrass/ggc/var/log
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      # MQTT TLS port
      - "8883:8883"
      # Stream Manager port
      - "8088:8088"
      # Sensor Data API port
      - "8180:8180"
    entrypoint: "/greengrass-entrypoint.sh"