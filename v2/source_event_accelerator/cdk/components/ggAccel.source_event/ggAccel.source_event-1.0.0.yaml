# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
# SPDX-License-Identifier: MIT-0
---
RecipeFormatVersion: 2020-01-25
ComponentName: ggAccel.source_event
ComponentVersion: "1.0.0"
ComponentDescription: Demonstrates processing IPC events similar to AWS Lambda
ComponentPublisher: Amazon
ComponentConfiguration:
  DefaultConfiguration:
    Message: world
Manifests:
  - Platform:
      os: linux
    Lifecycle:
      Run: |
        python3 -u {artifacts:decompressedPath}/ggAccel.source_event-1.0.0/handler.py '{configuration:/Message}'
    Artifacts:
      # Contents of component/artifacts/component_name/version will be compressed to component_name-version.zip
      # Variable-based URI: s3://${COMPONENT_BUCKET}/${ARTIFACT_KEY_NAME} is equivalent (must pass in both variables to the CDK construct)
      - URI: s3://${COMPONENT_BUCKET}/ggAccel.source_event-1.0.0.zip
        Unarchive: ZIP
